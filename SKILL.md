---
name: option-helper
description: 查询、解释、比较、推荐和计算期权及期权挂钩结构化产品。用于普通期权、价差、障碍、二元、触碰、雪球、安全气囊、累购、FCN、DCN、凤凰、鲨鱼鳍、方差和多标的结构的条款说明、收益结构、估值、Greeks、历史回测、数据获取及研究简报、完整研究报告或参考报价交付。用户询问产品结构、损益、风险、市场观点下的结构推荐、定价、回测、参考报价、交付物或数据时使用。
---

# OptionHelper

OptionHelper是供当前对话大模型调用的期权研究Skill。大模型负责理解用户意图；本Skill负责产品事实、受控合同、确定性模块和可验证结果。

## 文档职责与优先级

发生表述差异时按以下顺序执行：

1. 本文件决定用户意图路由、追问、默认值、进度和交付边界。
2. [全局术语与边界](CONTEXT.md)定义事实源、合同、数据和运行对象。
3. 当前任务对应的`module-guide.md`定义该模块输入、输出和拒绝条件。
4. [安装与配置说明](README.md)只负责安装、依赖、iFind与Store，不改变运行行为。
5. OptionList、OptionLib和Knowledger规则只提供产品事实或资料维护约束，不负责对话路由。

若低优先级文件与高优先级文件冲突，停止执行冲突部分并按高优先级规则说明缺口，不自行选择有利解释。

## 首次配置与按需就绪

纯咨询、条款解释和不调用模块的产品比较，只读取OptionList、OptionLib或已提供资料，直接回答；不要求Python、项目Store或iFind。不要为了回答“什么是看涨价差”先要求选择环境或配置数据。

以下运行时动作才需要就绪检查：DataFetcher、Payoffer、Pricer、Backtester、Recommender、Reporter、Designer，以及任何项目级CLI。运行入口自动初始化安装目录之外的项目Store：当前项目的`$PWD/.optionhelper/runtime`、`$PWD/data`和`$PWD/result`，并分别通过`OPTIONHELPER_RUNTIME_ROOT`、`OPTIONHELPER_DATA_ROOT`和`OPTIONHELPER_RESULT_ROOT`注入。安装目录只读；数据、结果、会话和凭据只能进入安装目录之外的项目Store。

首次需要运行Python时先确定解释器。若用户已在当前对话明确指定环境或绝对解释器路径，或运行环境已明确设置`OPTIONHELPER_PYTHON`，直接复用，不重复询问；否则先只读枚举本机可用的Conda环境、当前虚拟环境和PATH中的独立解释器，去重后展示环境名称、可安全取得的Python版本和解释器绝对路径，请用户选择。不得自行选择第一个、当前、base或任何看似可用的环境；用户明确选择前不得运行候选解释器、依赖检查或模块。没有Python3.11或更高版本候选时，说明缺口并询问是否授权安装或恢复一个受支持环境；得到授权后才使用本机安装方式，完成后重新枚举和选择，不得静默安装或改用系统Python。

用户选定后，本次任务的全部Python操作必须持续使用同一个解释器绝对路径，包括依赖检查、配置检查、DataFetcher、单模块运行、参数重算、Recommender、Reporter、Designer和Python子进程。每条Python命令都显式使用`"$OPTIONHELPER_PYTHON"`，不得改用裸`python`、`python3`、另一个Conda环境或系统解释器。需要安装依赖时也只能运行`"$OPTIONHELPER_PYTHON" -m pip`；只有当前环境无法继续且用户明确同意更换时，才重新选择并从检查开始。

首次需要数据、依赖新市场数据的计算，或首次需要取数的正式交付时，运行：

macOS/Linux（Bash）：

```bash
"$OPTIONHELPER_PYTHON" "$OPTIONHELPER_SKILL_ROOT/scripts/environment_check.py" \
  --check-readiness --skill-root "$OPTIONHELPER_SKILL_ROOT" --project-root "$PWD"
```

Windows（PowerShell）：

```powershell
& $env:OPTIONHELPER_PYTHON (Join-Path $env:OPTIONHELPER_SKILL_ROOT "scripts\environment_check.py") `
  --check-readiness --skill-root $env:OPTIONHELPER_SKILL_ROOT --project-root (Get-Location)
```

该完整门禁一次检查Python与锁定依赖、iFind数据API和外部Store。当前对话负责理解用户意图；iFind预检只检查项目级`.optionhelper/memory.md`是否已安全配置`IFIND_REFRESH_TOKEN`，不显示、记录或转述凭据，不要求用户维护Access Token，也不发起网络或数据请求。用户明确确认在本机保存后，后续需要数据的任务自动复用该项目配置。已有合同的Payoffer和基于已验证事实的重新渲染只检查已选Python、依赖和Store，不要求完整iFind门禁。`scripts/tool_entry.py --list`只列出入口，不是环境或数据能力检查。

首次需要保存iFind凭据时，在项目目录运行一次以下命令，并在安全输入提示中粘贴Token：

macOS/Linux（Bash）：

```bash
"$OPTIONHELPER_PYTHON" "$OPTIONHELPER_SKILL_ROOT/scripts/environment_check.py" \
  --save-ifind-refresh-token --project-root "$PWD"
```

Windows（PowerShell）：

```powershell
& $env:OPTIONHELPER_PYTHON (Join-Path $env:OPTIONHELPER_SKILL_ROOT "scripts\environment_check.py") `
  --save-ifind-refresh-token --project-root (Get-Location)
```

该命令只在用户明确确认保存后执行，写入项目目录`.optionhelper/memory.md`。后续检查会自动复用，不再重复询问。文件是明文本地配置，不是加密保险箱；不会进入Skill安装目录、发行包、日志、报告或版本库。

若Python版本不支持、库缺失`missing`或版本不符`version_mismatch`，停止受影响的运行时动作，一次列出每个库的锁定版本、当前版本和状态，并询问用户是否在已选环境安装锁定依赖。未经明确授权不得自动运行`pip install`、擅自换环境或用临时脚本绕过；只有用户明确授权安装后，才可在已选解释器中安装锁定依赖，并默认使用清华PyPI镜像`https://pypi.tuna.tsinghua.edu.cn/simple`。镜像不可用时保留原始错误，不自动切换其他软件源。安装后必须重新运行相关就绪检查。iFind未配置只阻止DataFetcher、依赖新市场数据的Pricer或Backtester，以及首次需要取数的推荐与正式交付；不阻止纯咨询、已有合同的Payoffer或基于已验证事实重渲染交付。Store未配置只阻止需要读写正式运行或交付物的动作。缺口出现时保留用户已确认的研究条件，不得以临时脚本替代。

当前对话Agent模式下：

- 用户已经指定产品时，只验证该产品、标的和参数，不再进行结构推荐。
- 用户要求推荐时，当前Agent必须始终站在客户一侧，根据客户表达的市场情景、投资期限、损失承受能力、本金风险偏好和收益目标筛选适合客户的产品。推荐尺度是客户的收益风险适配，不是发行便利、销售偏好、产品库存、页面默认值或示例编号；不得把不适合客户的高票息、复杂或高风险结构包装成推荐。
- 当前Agent必须按[Recommender指南](modules/recommender/module-guide.md)完成Intent、Research和Critic步骤，形成有资料依据的主候选；不得凭记忆直接选择产品。
- 项目级CLI的自然语言正式交付入口是`--project-request`，每次只生成一份HTML交付。`--project-json`只接收宿主已验证的结构化选择，不是模型自由拼装参数的入口。无论由哪一入口触发，数据、计算与交付都必须经过受控链路；不得拆写临时Python、手工HTML或第二套执行脚本，也不得以“快速版本”或“先做示例”为由创建临时Python、HTML、SVG或PDF。

## 任务能力边界

需要运行时能力时，先确认与本次动作相符的就绪条件，不重复索取已确认的数据凭据。当前任务已有覆盖标的、日期、字段和口径的受控`DataAssetRef`时直接复用；需要新数据时由DataFetcher使用已配置的iFind。路径型Pricer自动准备历史行情和独立未来交易日历，普通用户不填写iFind字段。iFind调用失败时分别说明凭据失效、网络不可用、Provider异常或数据权限不足；不得猜测数据、无提示回退本地CSV、索取Access Token或要求物理路径。Agent不得读取、解析或修改`datafetcher-cache`、`index.json`、`calendar-index.json`或其中资产文件，也不得用历史行情日期、普通工作日或临时脚本替代未来交易日历。

## 对话与默认值

- 先复用当前对话已经确认的标的、方向、期限、风险约束、合同参数和交付偏好。不得因为进入新模块、确认候选或生成交付物而重复询问。
- 只询问会改变候选范围、合同条款、计算含义或用户明确要求的交付覆盖的问题。多个关键缺口一次合并提出，不逐字段连续追问。
- 用户明确说“改成3个月重新估值”“波动率改为20%后重算”时，该指令本身就是确认。展示一次简短变更摘要后直接运行，并生成新的不可变合同和结果。
- 可以直接采用的操作默认值：当前对话环境、项目默认Store、iFind实时数据源、用户未填写时以今天作为请求估值日并披露实际采用的最新可用数据日、HTML格式、完整研究报告连续版。
- 请求日尚无可用数据，或遇到周末、节假日时，不等待也不要求用户稍后重跑。请求估值日保留用户输入或今天；实际估值日、合同起始参考价日和新发合同起始日统一采用DataAssetRef中所有标的均有覆盖的最新数据日，并在结果中同时披露请求日期与实际采用日期。不得把数据请求区间的截止日误当成实际数据日。
- 不能静默假设的金融条件：标的、方向、会改变候选的期限、最大可承受损失、本金风险及关键障碍、票息或参与率。面向用户的估值、回测和报告只展示百分比。`S0Raw`仅用于真实价格与标准化合同换算，不作为面向用户字段；内部现金流、点数、金额和名义本金不得投影到页面、正式Tool、CSV或报告。不询问名义本金，也不承诺实际交易金额。用户明确允许使用标准示例时才可采用示例条款，并说明其不构成报价。
- 用户已经提出“报告”“简报”“详细报告”“两份都要”“参考报价”或明确格式时，交付偏好已经确认，不再询问同一事项。参考报价由当前对话根据已确认需求确定结构、标的和期限组合，必要时形成多个合同版本并完成定价；Reporter只整理这些已验证运行结果，不把模型文字替代为报价。
- 用户未提出报告时直接完成分析，不主动询问是否需要报告，也不调用Reporter或Designer。
- 用户提出报告但未指定格式时直接生成HTML；完整研究报告未指定版式时使用连续版。不得为了确认默认格式或版式新增一轮对话。
- 模块之间交换结构化JSON。面向用户先给专业结论、依据、口径、限制和风险；只有用户明确要求JSON或进行系统集成时才展示原始JSON，且不展示内部标识或Tool名。

## 三种用户工作流

完成意图判断后，按用户表述命中以下三类之一；在真正运行模块前再核对相应就绪条件。不得把业务追问变成问卷，也不得把报告请求降级为模型自行写HTML。

### 强制执行门禁

每次请求先选定且只选定一种工作流，再执行其必经路径。模型不得把工作流中的任一步替换成自行编写的Python、HTML、PDF、图表或文件；模块返回的JSON只在模块之间传递，不能原样拼接成面向用户的报告。用户修改期限、波动率、行权价或其他合同参数时，先冻结新合同，再重跑受影响模块，绝不把旧结果贴到新参数下。

- 命中工作流1：直接给出研究结论；只有需要收益情景时才调用Payoffer。
- 命中工作流2：只调用用户指定或受参数变更影响的模块；用户明确要求交付时，把本次明确选择的已验证`ModuleRunRef`交给Reporter→Designer，不重新推荐、取数或计算。已有参考报价可组合多个已保存的运行结果，不要求来自同一份既有报告或同一轮计算。
- 命中工作流3且用户要求参考报价：执行`Recommender→必要的DataFetcher→各参数版本Pricer→Reporter→Designer`，一次形成组合Quote。默认由当前对话决定应纳入的结构、标的、期限和条款版本；用户明确指定某些结构、期限、条款或已保存结果时，按其指定覆盖对应部分。不得要求用户先生成多份旧结果再手工选择或拼接。研究简报和完整研究报告按所需模块执行，再交给Reporter→Designer。模块失败时按覆盖状态决定`partial`或`unavailable`，不得自制替代报告或把部分覆盖说成完整。
- 只有用户明确要求报告时才进入Reporter；进入Reporter后，只有Designer可以生成HTML或PDF。

| 工作流 | 触发条件 | 必经路径 | 只在何时提问 | 绝对禁止 |
| --- | --- | --- | --- | --- |
| 1.咨询与结构解释 | 科普、条款、收益机制、损益情景、产品比较 | OptionList/OptionLib；必要时Payoffer | 缺少标的或条款且无法解释时 | 无需数据却取数、估值、回测或生成报告 |
| 2.指定模块与参数重算 | 明确要求收益图、估值、Greeks、回测、取数、已有结果交付，或修改已确认参数 | 按已选ResolvedContract调用DataFetcher、Payoffer、Pricer或Backtester；明确要求交付时Reporter→Designer。已有参考报价可组合多个已保存结果 | 缺少会改变计算含义的金融条件时，一次合并问完 | 顺带运行未要求模块；为交付重新推荐、取数或计算；自行新建Python、HTML或计算脚本 |
| 3.结构推荐与正式交付 | 请求推荐；未指定结构的首次参考报价；或要求由系统选择结构后再交付 | Recommender→必要的数据与计算→Reporter→Designer | 多个同等候选、关键风险条件缺失或用户要求比较时 | 跳过Recommender；直接使用示例产品；不经Reporter→Designer自行输出文件 |

### 1.咨询与结构解释

先读[OptionList](references/optionlist.md)，再按需读[OptionLib](references/optionlib.md)。能够直接回答就直接回答；需要说明收益机制时可以调用[Payoffer](modules/payoffer/module-guide.md)，但收益图或情景不是报告。

### 2.指定模块与参数重算

用户已指定产品时，不做推荐；先复用同一产品、同一参数下的当前合同。没有匹配合同而缺少关键条款时，一次展示拟采用条款、来源和默认值，取得一次确认后再冻结合同。用户说“改成3个月”“波动率改为20%”“把行权价调为105后重算”即为已确认的参数覆盖：简短回显变化后，重新冻结合同并只运行受影响模块。

| 目标 | 模块指南 | 正常返回 |
| --- | --- | --- |
| 获取或复用行情数据 | [DataFetcher](modules/datafetcher/module-guide.md) | 数据覆盖、质量结论和受控DataAssetRef |
| 收益结构或收益图 | [Payoffer](modules/payoffer/module-guide.md) | 条款、收益情景、边界和图形资产引用 |
| 估值或Greeks | [Pricer](modules/pricer/module-guide.md) | 估值、Greeks、情景、精度和限制 |
| 历史回测 | [Backtester](modules/backtester/module-guide.md) | 样本范围、收益风险统计、路径结果和限制 |

用户已指定产品并首次要求研究简报或完整研究报告时，不进入Recommender：先形成该产品的合同快照，按交付需要取得数据并运行必要模块，再交给Reporter→Designer。用户未指定产品、要求系统推荐，或首次要求由系统组合参考报价时，进入工作流3。已有同一合同下的验证结果时，直接交给Reporter→Designer，不重新推荐、取数或计算。用户只要说“推荐并生成Quote”“给我报价”或同等意思，当前对话就自行决定应纳入的多个结构、标的、期限和参数版本，并把每一行完成必要定价后汇入同一份报价表；不要求用户逐行选择候选、期限、旧结果或拼接方式。用户也可以明确指定任意部分，例如只保留3个月和9个月、增加某个标的、替换一行条款，或要求复用已保存结果；系统只覆盖该部分后重新组织同一份Quote。Reporter冻结事实并交给Designer渲染，不重新取数、重新计算或进行结构推荐。不同结构不要求来自同一份既有报告；Designer不会重跑任何模块。结果不足时按`coverage=partial`或`coverage=unavailable`返回真实状态。

### 3.结构推荐与正式交付

执行[Recommender](modules/recommender/module-guide.md)的Intent、Research、Critic和聚合门禁。所有候选都从客户需求出发评估，先说明为什么适合客户当前情景、客户承担什么成本和风险、在哪些情况下不适合。用户只要推荐时，不主动推销报告。

Recommender选出候选后，研究简报和完整研究报告在缺少会改变合同含义的条件时，才向用户展示一份拟采用条款摘要并一次合并确认。参考报价默认由当前对话根据已确认需求直接确定组合，并由受控合同解析器冻结每个版本；用户要求逐候选、逐期限或逐条款确认时，再按其要求展示和调整。所有条款都来自受控合同解析器和用户已确认输入，默认值明确标为拟采用参数，不得表述为市场报价或可成交条件。

研究简报和完整研究报告在已提出条款摘要时，用户回复按此继续、无需调整或同等明确表达后，才冻结CandidateContract并运行后续模块。用户直接给出调整值时，该回复同时构成条款确认；更新合同并简要回显变更，不再重复询问。参考报价直接冻结当前对话决定的多个合同版本。交付格式和报告类型已经明确时不得再次询问。

默认路径为：确认研究条件→Recommender按客户利益筛选候选→冻结合同→按用户当前需要运行模块→Reporter冻结事实→Designer按固定模板输出。研究简报和完整研究报告仅在关键合同条件缺失时做一次合并确认；首次参考报价直接按当前对话决定的组合运行必要定价。已有同一合同下的验证结果时直接复用，不扫描目录选择“最新结果”。

多个候选同等适合时，先按客户承担的成本、最大损失、收益上限和路径风险说明差异。参考报价默认纳入当前对话认为合适的组合；用户要求比较或指定取舍时再调整。研究简报和完整研究报告仅在确有关键合同缺口时一次合并确认，不新增许可式提问。任一模块失败时保留已验证结果并标记覆盖不完整，不把部分覆盖表述为全量完成。

## 交付物与覆盖状态

对用户使用“研究简报”“完整研究报告”和“参考报价”。“简单报告”“简报”归入研究简报；“详细报告”“深度报告”“完整报告”归入完整研究报告。不要向普通用户解释`card`、`report`、`quote`、版式枚举或内部请求字段。

| 交付物 | 固定内容顺序 | 默认形式 |
| --- | --- | --- |
| 研究简报 | 1.结构推荐；2.推荐理由；3.关键合同条款；4.估值摘要；5.回测摘要；6.主要风险 | `card`；HTML或PDF；无章节目录；无损益图；HTML固定210mm宽度、高度随完整内容自然延展，不保留空白占位；PDF按A4分页 |
| 完整研究报告 | 1.核心结论；2.结构推荐；3.合同参数；4.收益结构；5.估值定价；6.历史回测；7.风险提示 | `report`；连续A4正文；宽屏HTML提供左侧七章目录，PDF不显示目录；正文均按同一顺序连续呈现 |
| 参考报价 | 当前推荐或已保存的多个合同版本的期限、执行价、期权费和必要障碍或观察条款，按产品或结构分组展示 | `quote`；HTML或PDF；不展示Greeks、损益图、估值图或回测图；首次可直接生成多结构、多标的、多期限或多参数版本组合，后续也可复用多个已保存运行结果 |

交付形式与分析覆盖是两个概念：

- `coverage=complete`：本次用户要求的全部模块均有同一合同下的已验证结果。
- `coverage=partial`：报告可以基于已验证结果生成，但必须明确未纳入的模块和影响，不得声称全面覆盖。
- `coverage=unavailable`：没有足够已验证结果，或Reporter、Designer不可用；不生成替代文件。

研究简报和完整研究报告基于用户选定的一个已验证合同快照生成。用户可以随时修改条款后以新合同版本单独重跑Payoff、Pricing或Backtest，旧版与新版结果均保留；也可以在不重跑任何模块的情况下，从已有快照直接补生成研究简报、完整研究报告或另一种格式。参考报价首次可在推荐流程中直接形成多结构、多标的、多期限或多参数版本的组合；后续也可从用户明确选择的多个已保存运行结果生成，按各自合同参数整理，可跨任务、跨标的、跨结构。具备正式Reporter和Designer端口的运行入口可交付HTML或PDF；项目级独立CLI可生成组合参考报价HTML。能力不足时只说明当前缺少的能力，不静默降级或伪造交付。

Reporter负责选择或复用交付事实集、覆盖状态和固定章节编排；Designer只负责固定模板、字体、公式、数字格式及HTML/PDF渲染，不重算金融结果。交付事实集可以是本次从已保存快照选出的新组合，也可以是之前保存的事实集；后一种直接重渲染为另一种交付形式，不触发重新计算。HTML成品本身不是可验证事实来源，但用户始终可以回到任意保存快照，修改条款并单独运行需要的模块。Report七个标题必须逐字使用“核心结论、结构推荐、合同参数、收益结构、估值定价、历史回测、风险提示”，不得改写、删减或换序。报告正文不展示系统品牌、Tool名、文件路径、运行标识、审计过程或内部机器字段。任何缺失模块都只在其固定章节内说明未纳入，不得删除、换序或补造章节。

用户要求打开交付物时，在Reporter→Designer成功后由当前宿主预览该受控HTML或PDF；不得为打开文件重新计算、改写HTML或另建临时页面。当前宿主不能打开时，直接返回交付路径和真实原因。

风险提示按任务事实从`assets/reporter/disclaimer.md`中选取并压缩，不机械复制全文，也不得删除与本次结构直接相关的主要风险。

## 进度所有权

- 单独调用模块时，使用对应模块指南的开始、进度、完成或失败文案。
- 组合分析和正式交付时，只由顶层工作流向用户发进度：开始整理条件、正在计算验证、正在生成交付。子模块进度只记录，不逐条重复展示。
- 同一任务最多发送1条开始提示和1至2条必要进度，不要求用户回复。失败时一次说明失败阶段、已完成部分、受影响结论和下一步。

## 不可违反的边界

- OptionList是人工目录，OptionLib是条款与唯一数学符号主源，OptionReg是唯一机器产品源。三者冲突时停止并报告。
- 只通过共享合同解析器生成ResolvedContract，不得自行拼装条款、路径或现金流。
- 安装目录只读。数据、结果、凭据和会话必须位于安装目录之外的项目Store；不得把密钥写入提示、日志、结果或报告。
- 固定默认Payoffer JSON和SVG只读。运行产物只写入外部ResultStore，不把示例数值表述为真实报价、承诺或可成交条件。
- 普通研究、咨询、计算和报告任务不得新建或编辑宿主项目中的`.py`、HTML或其他源码。只能调用Skill已经提供的现成模块。只有用户明确要求开发或修改代码时，才可在授权范围内修改源码。
- 每一份交付都以Reporter选定或复用的交付事实集为事实来源，再由Designer生成HTML或PDF。这是交付路径，不是计算限制：用户可随时修改条款，单独运行任一模块，并从旧版或新版合同快照选择生成Card、Report或Quote。已保存的事实集可直接补生成研究简报、完整研究报告或转换格式，无需重新取数、定价、回测或再次编排事实。Reporter或Designer不可用时，明确说明报告能力不可用、覆盖缺口和下一步，不以临时结果伪造正式交付。
- 资料不足、权限不足、依赖缺失或产品不可执行时返回真实状态，不伪造成功结果。

## 返回结果

先给结论，再给依据、条款或计算口径、限制和风险。使用OptionLib规定的数学符号和专业自然语言，不展示未解析机器键、内部运行名或物理路径。
