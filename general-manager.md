# OptionHelper总管理规范

## 1. 职责

本文件管理跨模块架构、命名、目录归属、共享协议、构建、发布与冲突裁决。它不修改单产品金融条款，不代替Knowledger Manager或模块Manager。

## 2. 修改顺序

1. 先确认变更属于Knowledger、共享核心、单模块、App、构建或设计系统。
2. 评估受影响的接口、结果协议、默认资产、测试和发布物。
3. 共享协议变更先修改`core/`契约和测试，再迁模块；模块不得自行复制上游逻辑。
4. 目录、Import或入口迁移必须保留可复现实例和回归证据。
5. 新能力进入发行前必须通过模块测试、跨模块测试和安全边界检查。

## 3. 归属裁决

| 内容 | 唯一归属 |
| --- | --- |
| 产品身份、条款、路径、默认资产 | `references/`与Knowledger版本 |
| 合同、监测、日程、现金流、协议、端口 | `core/` |
| 数据、推荐、收益图、定价、回测、报告、设计能力 | 对应`modules/{module}/` |
| 登录、权限、模型网关、会话、任务、App壳层 | `products/app/` |
| 构建白名单、验收、Skill/App打包 | `packaging/` |
| 本地数据、结果、版本与发行产物 | `data/`、`result/`、`versions/`、`dist/` |

## 4. 关键禁止事项

- 不在页面、对话、Reporter或模块中重写产品现金流。
- 不让Payoffer、Pricer、Backtester互相调用或用彼此结果补值。
- 不让模块直接读取硬编码的OptionReg路径；必须经Registry Loader。
- 不把默认资产、测试、历史或开发Manager误打入普通Skill发行包。
- 不因迁移、优化或目录整理覆盖默认资产、历史结果或基座数值证据。

## 5. 冲突裁决

产品金融事实以OptionLib与经核对的OptionReg为准；系统边界以总蓝图和本文件为准；实际完成度以当前代码、测试和运行证据为准。历史文件只可解释演进，不可恢复为并列规范。

## 6. 验收

每次跨模块变更至少证明：目录归属唯一、Import稳定、契约兼容、结果目录正确、默认资产未改写、受影响测试通过、没有新增未授权的持久化事实源。

## 7. 构建、版本与回滚

开发源只能经`packaging/skill/package-source-map.json`进入Skill候选包。候选包先验证文件集合、链接、Schema、Secret、目录树哈希、模块哈希和金融基线，再原样复制为App Capability。Skill与App可独立发布和回滚；历史版本只读，不用新构建覆盖旧版本。

## 8. 后续对话交接

每个模块对话先读总蓝图、`CONTEXT.md`、本模块Guide与Manager，再核对当前代码和测试。修改仅限该模块；发现上游协议问题时停止并提交变更，不得在模块内建立兼容副本。
