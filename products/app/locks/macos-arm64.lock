# Planned macOS arm64 dependency lock.
# This source-stage file intentionally contains no resolved third-party wheel.
# Populate only after the selected Python WebView runtime, signing toolchain and
# capability build have passed macOS acceptance. Do not reuse this lock on Windows.
platform = "macos-arm64"
status = "not_resolved"
python = ">=3.11"
